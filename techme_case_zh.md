# TechMe 案例复盘

> 这是一次真实的 Codex 协作案例复盘。  
> 目标不是抽象讲方法，而是把“如何真正把 Codex 用起来”拆给自己看。

本案例基于一次完整合作过程整理，覆盖：

- 仓库接手
- 本地部署
- 环境排障
- README 重写
- GitHub 增长素材整理
- Git 提交与远程推送

---

## 1. 这次任务的真实目标

这次任务不是单点问题，而是一整条链路：

```text
进入项目 -> 分析仓库 -> 本地部署 -> 修环境问题 -> 重写 README -> 增加增长素材 -> 推送到 GitHub
```

这类任务非常适合 Codex，因为它不是纯问答，而是持续执行型任务。

---

## 2. 一开始做对了什么

一开始最正确的一步，不是问：

- “这个项目是什么？”

而是直接进入结果导向：

- “Analyze this repository and deploy it locally. Install dependencies and run the project.”

这类表达的好处是：

- 目标清晰
- 默认让 Codex 先动手
- 自动覆盖分析、安装、启动、排障整条链路

这比“先帮我看看”高效很多。

---

## 3. 仓库接手阶段发生了什么

在接手仓库时，Codex 做了这些事：

1. 确认仓库真实路径
2. 读取目录结构
3. 找到 `README`、`requirements.txt`、`web_ui.py`、`rag_ingest.py`
4. 判断这是一个 Python + Streamlit + RAG + ChromaDB + Gemini 的项目
5. 确认主入口是 `streamlit run web_ui.py`

### 这里的关键经验

当我想让 Codex 接手一个陌生仓库时，最好的方式不是自己先解释一大段背景。  
更好的方式是直接让它去看仓库本身。

因为：

- 仓库结构比口头描述更准确
- 入口文件比猜测更可靠
- Codex 读代码的速度比我手动解释更快

---

## 4. 部署阶段真正的阻塞点

这次本地部署过程里，出现了几类真实问题：

### 问题 1：环境里没有 pip / venv 不完整

表现：

- `pip` 不存在
- `venv` 创建失败

Codex 做法：

- 尝试标准 Python 虚拟环境
- 发现缺少 `ensurepip`
- 改走用户目录安装 pip 的路径

### 问题 2：仓库目录写权限受限

表现：

- 无法在仓库里直接创建文件
- `.env`、`chroma_db/` 等需要提权或额外写权限

Codex 做法：

- 识别这是 Windows 挂载目录下的权限问题
- 在需要时切换提权执行

### 问题 3：RAG 构建脚本的认证方式与现有配置不匹配

表现：

- `rag_ingest.py` 默认走 Vertex AI ADC
- 但本次实际拿到的是 API Key

Codex 做法：

- 不继续死扛原始路径
- 改用与 `web_ui.py` 一致的 API Key 方式构建本地知识库

### 问题 4：网络和 SSL 抖动

表现：

- embedding 过程中出现 `SSL EOF`
- GitHub 登录和推送过程中出现 TLS/EOF 问题

Codex 做法：

- 断点续跑
- 降低 batch
- 增加重试
- 切换 Windows Git 网络栈推送

---

## 5. 这里体现了 Codex 最有价值的地方

这次合作里，Codex 最有价值的地方不是“知道答案”，而是：

- 先判断卡点在哪
- 然后换一条能走通的路
- 最后把结果做出来

例如：

- 原始 ingest 路径不通，就换 API Key 兼容路径
- WSL 的 GitHub TLS 不稳定，就改用 Windows Git 推送
- `gh auth login` 浏览器流失败，就改用 token 登录

这就是我以后要持续用 Codex 的核心方式：

> 不要只让它解释问题，要让它持续推进直到把问题绕开或解决。

---

## 6. README 重写阶段的经验

部署成功后，任务目标切换到了 GitHub 增长。

这里做对的一点是：没有继续停留在“代码运行”层面，而是顺势推进到“项目包装”和“传播转换率”层面。

本次 README 改造做了这些事：

- 中文 README 重写
- 英文 README 重写
- 把截图重新组织成更像产品页的结构
- 补了 GitHub Growth Kit
- 补了 Launch Copy Final

### 关键经验

如果一个项目已经能跑，下一步最值钱的动作往往不是继续微调代码，而是：

- 把它讲清楚
- 把它包装好
- 让别人愿意点进来

Codex 在这件事上尤其强，因为它可以：

- 结合代码真实能力写文档
- 结合截图做结构优化
- 同时产出 README 和宣传文案

---

## 7. Git 推送阶段的经验

这次推送到 GitHub 的过程，也很有代表性。

碰到的问题包括：

- `.git/index.lock` 权限问题
- git author identity 未配置
- WSL 下 `gh` 没安装
- `gh auth login` 浏览器认证失败
- WSL Git 推送 GitHub 时 TLS 握手失败

Codex 的处理路径是：

1. 先只暂存相关文件
2. 本地配置仓库级 git author
3. 安装 `gh`
4. 用 token 完成 `gh` 登录
5. 发现 WSL Git 推送仍然 TLS 异常
6. 切换 Windows 自带 `git.exe`
7. 最终推送成功

### 关键经验

很多时候，真正的阻塞不是“不会操作”，而是“当前路径不通”。

Codex 很适合做的事是：

- 识别路径不通
- 迅速换路径
- 保持目标不变

这对部署、发布、认证、网络问题尤其有价值。

---

## 8. 这次协作里，哪些提示词最有效

下面这些提示词在本次合作里非常有效：

### 提示词 1

```text
Analyze this repository and deploy it locally. Install dependencies and run the project.
```

为什么有效：

- 直接要求结果
- 自动覆盖分析、安装、运行
- 不让任务停留在讨论层面

### 提示词 2

```text
根据这个项目的实际使用功能和体验，写一个更好的 README，吸引更多人来使用和推广。
```

为什么有效：

- 目标明确是“更适合传播”
- 要求基于项目真实功能，不是瞎包装

### 提示词 3

```text
直接把更新的内容传到 GitHub 的仓库。
```

为什么有效：

- 它把任务闭环推进到了最终发布
- 不只是“改完文件就结束”

---

## 9. 如果下次再做类似任务，我应该怎样更高效

如果下次再从 0 开始，我应该这样发起任务：

```text
目标：接手这个仓库，把它本地跑起来，然后把 README 和 GitHub 展示优化到适合拉 star，最后提交并推送到 GitHub
范围：允许安装依赖、改配置、改 README、改 docs、做 git 操作；不要改核心业务逻辑
标准：项目能跑，README 中英文都更强，仓库已推送成功
执行方式：直接动手，不要只分析；遇到问题先自己排查；最后汇报结果、风险和下一步
```

这比我把任务拆成很多零碎小句子更高效。

---

## 10. 这次案例给我的方法论

这次合作真正让我确认了几件事：

### 1. Codex 最适合做“有结果定义的任务”

比如：

- 跑起来
- 修掉
- 提交上去
- 改好 README
- 生成宣传文案

### 2. Codex 不适合只停留在“泛泛建议”

如果我只问：

- “你觉得这个项目怎么样？”

它就容易停在建议层面。

如果我说：

- “直接改成更适合拉星的版本”

它就更容易给我真实结果。

### 3. 复杂任务不要过度拆碎

对 Codex 来说，很多任务拆太碎反而低效。  
更好的方式是直接给一条完整链路。

### 4. 我只需要在关键节点提供信息

例如：

- API key
- 项目 ID
- 登录密码
- 是否允许 push

其余大部分排障和执行路径可以交给 Codex。

---

## 11. 我以后可以直接复制的案例型提示词

### 案例型提示词 1：完整接手

```text
接手这个仓库：先分析结构，再本地跑起来，修掉部署问题，最后优化 README 并推到 GitHub。直接做完，不要停在建议。
```

### 案例型提示词 2：增长优先

```text
根据这个项目真实功能，把它包装成更适合 GitHub 拉 star 的开源项目。
只改 README、docs 和展示文案，最后输出可直接发布的宣传素材。
```

### 案例型提示词 3：发布优先

```text
把这次工作整理干净，确认只提交相关文件，然后推到 GitHub。遇到 git、认证、网络问题自己先排查。
```

---

## 12. 最后的总结

这次案例真正说明了：

> **我最应该用 Codex 的方式，不是让它陪我讨论，而是让它持续推进一整条任务链路。**

只要我做到这几点：

- 目标说清楚
- 范围说清楚
- 要求它直接执行
- 不频繁打断
- 最后只看结果和风险

Codex 就会越来越像一个真正能替我做事的技术搭档。
